runtime: "typescript"
startCommand:
  type: "http"
  configSchema:
    type: "object"
    properties: {}
  commandFunction: |
    (config) => ({
      command: "node",
      args: ["dist/index.js"],
      env: {}
    })
